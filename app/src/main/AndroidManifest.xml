<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cathaybkandroidexercise">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CathaybkAndroidExercise">
        <activity
            android:name=".ui.UserListActivity"
            android:exported="true"
            android:label="@string/github_user_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.UserDetailActivity"
            android:theme="@style/Theme.CathaybkAndroidExercise.NoActionBar"
            >
        </activity>
    </application>

</manifest>